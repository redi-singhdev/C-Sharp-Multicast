Basically, this can send a series of files from one computer to your entire local network at the same time without clogging the tubes. It's the most elegant solution and is more of a proof-of-concept. 

It was designed to experiment with a morphing UI, where the client and server use the same interface, and change depending on requirements.
